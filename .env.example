# docker compose reads APP_PORT — the ONLY published port. Open http://localhost:7400
APP_PORT=7400

# --- local dev only (npm run web); docker compose does not use these ---
# REDIS_URL=redis://127.0.0.1:6379
# PRESENT_PORT=8080
# AGG_PORT=3001
# TRAINER_PORTS=3002,3003
